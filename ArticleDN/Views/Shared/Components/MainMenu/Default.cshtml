<li class="dropdown">
    <a asp-controller="Menu"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Menus</a>
    <ul class="dropdown-menu">
        @foreach (var menu in Model)
        {
            <li>
                @menu.MenuName
            </li>
        }
    </ul>
</li>